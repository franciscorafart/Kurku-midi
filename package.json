{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for Kurku app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "author": "Francisco Rafart",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "stripe": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
